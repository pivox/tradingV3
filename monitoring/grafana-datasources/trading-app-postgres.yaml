apiVersion: 1

datasources:
  - name: Trading App Postgres
    uid: trading-app-postgres
    type: postgres
    access: proxy
    url: trading-app-db:5432
    database: trading_app
    user: postgres
    secureJsonData:
      password: password
    jsonData:
      sslmode: disable
      postgresVersion: 1500
      timescaledb: false
