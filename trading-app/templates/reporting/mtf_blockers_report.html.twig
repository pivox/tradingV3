{% extends 'base.html.twig' %}

{% block title %}Reporting - Analyse Blockers{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h1 class="h3">
            <i class="bi bi-shield-exclamation"></i>
            Analyse MTF Blockers
        </h1>
    </div>

    <div class="alert alert-info">
        <p class="mb-1"><strong>Descriptif :</strong> lance l’analyse `scripts/analyze_mtf_blockers.php` pour identifier les rejets MTF sur une période.</p>
        <p class="mb-0"><strong>Exemple :</strong> sélectionnez une date (et éventuellement une heure minimale HH:MM UTC) puis cliquez sur <em>Actualiser</em> pour générer le rapport.</p>
    </div>

    <form method="get" class="row g-3 mb-4">
        <div class="col-auto">
            <label for="date" class="form-label">Date (UTC)</label>
            <input type="date" class="form-control" id="date" name="date" value="{{ date }}">
        </div>
        <div class="col-auto">
            <label for="time" class="form-label">Heure minimale (HH:MM UTC)</label>
            <input type="time" class="form-control" id="time" name="time" value="{{ timeFilter ?? '' }}" step="60">
        </div>
        <div class="col-auto align-self-end">
            <button type="submit" class="btn btn-primary">
                <i class="bi bi-arrow-clockwise"></i> Actualiser
            </button>
        </div>
    </form>

    {% include 'reporting/report_alerts.html.twig' with { report: report } %}

    <div class="card">
        <div class="card-body">
            {% if report.output %}
                <pre class="mb-0 text-break">{{ report.output }}</pre>
            {% else %}
                <p class="text-muted mb-0">Aucune donnée disponible pour cette requête.</p>
            {% endif %}
        </div>
    </div>
{% endblock %}
