#!/usr/bin/env php
<?php

use Symfony\Component\Console\Application;

require_once __DIR__.'/../vendor/autoload.php';

$application = new Application('WS Worker', '1.0.0');

// Créer la commande sans instancier les services lourds
$command = new \App\Command\WsWorkerCommand();

$application->add($command);

$application->run();
